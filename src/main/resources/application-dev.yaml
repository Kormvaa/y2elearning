spring:
  security:
    oauth2:
      resource server:
        jwt:
          issuer-uri: http://localhost:8080/realms/myrealm
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/db_elearning_a01_m1
    username: postgres
    password: qwer
  application:
    name: elearning-code-m1
server:
  port: 8081